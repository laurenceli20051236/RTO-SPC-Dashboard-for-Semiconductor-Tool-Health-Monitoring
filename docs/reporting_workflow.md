# Reporting Workflow

Phase 1 reporting is limited to CSV export from the Excursion Review page.

The UI reads `data/excursion_events.csv`, applies user-selected filters, and exports the filtered rows as CSV.

The dashboard does not recalculate OOC status in the UI.
