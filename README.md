# RTO SPC Dashboard for Semiconductor Tool Health Monitoring

A synthetic semiconductor manufacturing analytics dashboard for RTO tool-health monitoring, combining module-level thickness monitoring, particle-adder alerting, deterministic SPC logic, across-fleet visualization, and traceable excursion review.

---

## Overview

This project demonstrates how synthetic RTO monitor data can be transformed into an engineering-oriented tool-health dashboard using Python, Streamlit, Plotly, pandas, and pytest.

The dashboard is designed around two core RTO monitor categories:

### Thickness monitoring

* RTR Mean, X-BAR chart
* WIW Stdev, SIGMA chart

### Particle monitoring

* Total Adder
* Cluster Adder
* Large Particle Adder

The system calculates control limits from baseline data only, applies deterministic warning and OOC rules to monitoring data, and writes every detected event to a centralized excursion-event table.

---

## Why This Project Exists

RTO processes require tight process monitoring because relatively small thickness or uniformity changes can indicate meaningful changes in recipe stability, chamber condition, thermal behavior, metrology consistency, or contamination risk.

The project was created to demonstrate:

* Semiconductor process-monitoring knowledge
* RTO module-level engineering logic
* Statistical Process Control implementation
* Particle-monitor alert design
* Fleet-level tool and chamber comparison
* Traceable excursion-event architecture
* Python analytics and dashboard development
* Testing, documentation, and confidentiality discipline

---

## Core Design Principles

### RTO-only scope

The project is intentionally limited to synthetic RTO tool-health monitoring.

It does not attempt to model:

* Furnace operations
* FDC sensor traces
* APC feedback control
* Real FAB systems

### Baseline-only control limits

All control limits are calculated only from rows where:

```text
phase == "baseline"
```

Monitoring data is evaluated against frozen baseline limits.

Monitoring excursions are never allowed to modify the limits used to detect them.

### Per-stream SPC calculations

Control limits are calculated independently for each monitor stream:

```text
tool_id + chamber_id + recipe_group + metric_name
```

Data from different tools, chambers, recipes, or metrics is never combined to calculate a shared SPC limit.

### Fleet charts are visualization-only

Across-fleet charts compare multiple RTO tools and chambers, but they do not calculate fleet-wide control limits.

Each displayed stream preserves its own:

* Baseline MEAN
* Baseline SIGMA
* Upper Control Limit (UCL)
* Control Limit (CL)
* Lower Control Limit(LCL)
* Warning Status
* Out-Of-Control (OOC) Status

### Deterministic event detection

SPC and particle-event decisions are generated by deterministic rules.

Local summaries and suggested review areas do not determine SPC status and do not replace engineering review.

---

## Thickness Monitor Methodology

The Phase 1 RTO thickness monitor uses two primary SPC chart types:

| SPC chart | Underlying monitor metric | Engineering purpose                                                                             |
| --------- | ------------------------- | ----------------------------------------------------------------------------------------------- |
| X-BAR     | RTR Mean                  | Tracks run-to-run average thickness behavior and detects thickness mean shift or drift          |
| SIGMA     | WIW Stdev                 | Tracks within-wafer thickness variation and detects uniformity or process-variation degradation |

The internal metric names are:

```text
thickness_rtr_mean
thickness_wiw_stdev
```

The dashboard must not create duplicate metric streams named:

```text
thickness_xbar
thickness_sigma
```

### X-BAR chart

The X-BAR chart displays the RTR Mean for each synthetic RTO thickness-monitor run.

It is used to review:

* Run-to-run mean thickness behavior
* Process centering
* Gradual thickness drift
* Sudden mean shift
* Tool and chamber matching
* Metrology trend consistency

### SIGMA chart

The SIGMA chart displays the WIW Stdev for each synthetic RTO thickness-monitor run.

It is used to review:

* Within-wafer thickness uniformity
* Increasing wafer-level variation
* Chamber thermal uniformity
* Wafer placement or process-distribution changes
* Metrology repeatability

### Baseline-only Limits

Control limits for both chart types are calculated independently from baseline data for each:

```text
tool_id + chamber_id + recipe_group + metric_name
```

Monitoring observations are evaluated against frozen baseline limits.

Across-fleet charts are used for tool and chamber comparison only. They must not calculate a shared fleet-wide control limit.

## Particle Monitor Methodology

Particle monitoring uses synthetic engineering thresholds and repeated-event escalation.

It does not use c-chart or u-chart logic as the primary alert method.

| Metric               | Internal metric name     | Warning | High |
| -------------------- | ------------------------ | ------: | ---: |
| Total Adder          | `particle_total_adder`   |      10 |   20 |
| Cluster Adder        | `particle_cluster_adder` |       3 |    6 |
| Large Particle Adder | `particle_large_adder`   |       1 |    3 |

These thresholds are synthetic demonstration values and are not production limits.

### Particle Rules

| Rule                                 | Condition                                                                                       | Severity |
| ------------------------------------ | ----------------------------------------------------------------------------------------------- | -------- |
| `PARTICLE_THRESHOLD_WARNING`         | Value reaches warning threshold but remains below high threshold                                | Medium   |
| `PARTICLE_THRESHOLD_HIGH`            | Value reaches or exceeds high threshold                                                         | High     |
| `PARTICLE_REPEATED_EVENT_ESCALATION` | At least three warning-level observations occur within the latest ten points in the same stream | High     |

Repeated-event logic is evaluated independently within:

```text
tool_id + chamber_id + recipe_group + metric_name
```

---

## Dashboard Pages

### 1. Tool Health Summary

Provides an across-fleet view of:

* Synthetic tool-health scores
* Event counts by tool
* Event counts by chamber
* Severity distribution
* Latest high-severity events

> Synthetic demo health score, not a production disposition rule.

### 2. RTO Thickness Monitor

Provides fleet-level and selected-stream views for:

* RTR Mean
* X-BAR
* WIW Stdev
* SIGMA

The selected-stream view includes:

* Value trend
* UCL, CL, and LCL
* Warning markers
* OOC markers
* Associated event records

### 3. Particle Alerts

Provides fleet-level and selected-stream views for:

* Total Adder
* Cluster Adder
* Large Particle Adder

The selected-stream view includes:

* Particle trend
* Warning threshold
* High threshold
* Medium-severity markers
* High-severity markers
* Repeated-event escalation markers
* Associated event records

### 4. Excursion Review

Provides a centralized review workflow based on `excursion_events.csv`.

Views include:

* Full event table
* Event count by metric
* Event count by tool
* Event count by chamber
* Severity distribution
* Excursion timeline
* Filtered CSV export

### 5. Local Event Summary

Generates a deterministic event summary using stored SPC and threshold results.

The summary may include:

* Event ID
* Tool and chamber
* Monitor metric
* Triggered rule
* Severity
* Suggested review area

> This summary is generated from deterministic SPC / threshold results and does not replace engineering review.

---

## Across-Fleet Dashboard Design

Every major monitor type includes at least one across-fleet overview.

Required fleet views include:

### Thickness

* Fleet RTR Mean trend
* Fleet X-BAR trend
* Fleet WIW Stdev trend
* Fleet SIGMA trend
* Thickness-event counts by tool, chamber, and metric

### Particle

* Fleet Total Adder trend
* Fleet Cluster Adder trend
* Fleet Large Particle Adder trend
* Particle-event counts by tool, chamber, and metric

### Event review

* Event count by tool
* Event count by chamber
* Event count by metric
* Severity distribution
* Fleet excursion timeline

Fleet charts use precomputed per-stream results. They never generate shared fleet-level SPC limits.

---

## Data Pipeline

```text
Synthetic monitor-data generation
        ↓
monitor_measurements.csv
        ↓
Baseline-only per-stream limit calculation
        ↓
Thickness and particle rules
        ↓
spc_results.csv
        ↓
Warning and OOC event extraction
        ↓
excursion_events.csv
        ↓
Streamlit dashboard and local event summaries
```

---

## Required Data Outputs

### `data/monitor_measurements.csv`

Contains raw synthetic monitor measurements.

Core fields include:

```text
timestamp
phase
tool_id
chamber_id
recipe_group
monitor_type
metric_name
value
unit
lot_id_hash
monitor_wafer_id_hash
sequence_id
```

### `data/spc_results.csv`

Contains measurements plus calculated limits and rule results.

Core fields include:

```text
baseline_mean
baseline_sigma
ucl
cl
lcl
warning_flag
ooc_flag
rule_triggered
severity
```

### `data/excursion_events.csv`

Contains every warning and OOC event.

Core fields include:

```text
event_id
timestamp
tool_id
chamber_id
recipe_group
monitor_type
metric_name
value
ucl
cl
lcl
rule_triggered
severity
suggested_review_area
status
comment
```

Every warning or OOC row in `spc_results.csv` must produce a corresponding event record.

---

## Synthetic Dataset

The default synthetic dataset includes:

### Tools

```text
RTO_A01
RTO_A02
RTO_A03
RTO_A04
```

### Chambers

```text
CH1
CH2
```

### Recipe groups

```text
RTO_BASELINE
RTO_OXIDE_A
```

### Data phases

```text
baseline
monitoring
```

### Deterministic seed

```text
42
```

The data generator includes stable baseline behavior, stable monitoring behavior, warning-zone points, OOC thickness events, particle threshold events, and repeated particle-event patterns.

---

## Technology Stack

| Component       | Technology            |
| --------------- | --------------------- |
| Language        | Python                |
| Data processing | pandas, NumPy         |
| Dashboard       | Streamlit             |
| Visualization   | Plotly                |
| Testing         | pytest                |
| File format     | CSV, optional Parquet |
| Packaging       | `pyproject.toml`      |
| Version control | Git and GitHub        |

---

## Repository Structure

```text
rto-spc-dashboard/
│
├── README.md
├── requirements.txt
├── pyproject.toml
├── .gitignore
│
├── app/
│   ├── dashboard.py
│   └── pages/
│       ├── 1_Tool_Health_Summary.py
│       ├── 2_Thickness_Monitor.py
│       ├── 3_Particle_Alerts.py
│       ├── 4_Excursion_Review.py
│       └── 5_Local_Event_Summary.py
│
├── src/
│   └── rto_spc/
│       ├── __init__.py
│       ├── config.py
│       ├── schemas.py
│       ├── data_generator.py
│       ├── data_loader.py
│       ├── control_limits.py
│       ├── thickness_monitor_rules.py
│       ├── particle_rules.py
│       ├── excursion_scoring.py
│       ├── tool_health.py
│       ├── chart_helpers.py
│       └── ai_summary.py
│
├── data/
│   ├── monitor_measurements.csv
│   ├── spc_results.csv
│   └── excursion_events.csv
│
├── scripts/
│   └── generate_sample_data.py
│
├── tests/
│   ├── test_data_generation.py
│   ├── test_control_limits.py
│   ├── test_thickness_monitor_rules.py
│   ├── test_particle_rules.py
│   ├── test_excursion_scoring.py
│   └── test_fleet_charts.py
│
└── docs/
    ├── methodology.md
    ├── data_dictionary.md
    ├── confidentiality_note.md
    └── screenshots/
```

---

## Installation

### 1. Clone the repository

```bash
git clone <repository-url>
cd rto-spc-dashboard
```

### 2. Create a virtual environment

Windows:

```bash
python -m venv .venv
.venv\Scripts\activate
```

macOS or Linux:

```bash
python -m venv .venv
source .venv/bin/activate
```

### 3. Install dependencies

```bash
pip install -r requirements.txt
```

Alternatively:

```bash
pip install -e .
```

---

## Generate Synthetic Sample Data

Run:

```bash
python scripts/generate_sample_data.py
```

Expected outputs:

```text
data/monitor_measurements.csv
data/spc_results.csv
data/excursion_events.csv
```

---

## Run the Tests

```bash
pytest
```

The test suite validates:

* Required metrics exist
* Baseline and monitoring phases exist
* Limits use baseline data only
* Limits remain isolated by stream
* Thickness rules trigger correctly
* Particle thresholds trigger correctly
* Repeated-event logic remains group-specific
* Warning and OOC rows become excursion events
* Fleet charts do not calculate shared limits

---

## Launch the Dashboard

```bash
streamlit run app/dashboard.py
```

Streamlit should open the local dashboard at:

```text
http://localhost:8501
```

---

## Recommended Demo Path

For a short portfolio walkthrough:

1. Open **Tool Health Summary**
2. Review the fleet health and event distribution
3. Open **RTO Thickness Monitor**
4. Compare fleet RTR Mean, X-BAR, WIW Stdev, and SIGMA
5. Select one tool/chamber stream and review its limits
6. Open **Particle Alerts**
7. Review Total, Cluster, and Large Adder events
8. Open **Excursion Review**
9. Select an event and review the deterministic event summary

---

## Suggested Engineering Review Areas

The dashboard provides deterministic suggested-review text based on metric type.

Examples include:

| Metric               | Suggested review focus                                                  |
| -------------------- | ----------------------------------------------------------------------- |
| RTR Mean             | Recipe centering, run-to-run drift, thermal stability, metrology trend  |
| X-BAR                | Average thickness behavior, chamber matching, recipe stability          |
| WIW Stdev            | Within-wafer uniformity, wafer placement, thermal uniformity            |
| SIGMA                | Process variation, chamber stability, metrology repeatability           |
| Total Adder          | General contamination, chamber-clean history, wafer-transfer path       |
| Cluster Adder        | Localized particle source, chamber condition, repeated cluster behavior |
| Large Particle Adder | High-risk particle source, chamber recovery, PM effectiveness           |

These suggestions are review prompts only. They are not root-cause determinations.

---

## Synthetic Data and Confidentiality Disclaimer

This project uses fully synthetic and anonymized semiconductor monitor data for public portfolio demonstration only. It does not contain real tool identifiers, real recipe names, real product information, real wafer records, real lot records, real process limits, real SPC limits, or any confidential manufacturing information.

---

## What This Project Does Not Claim

* This project is not a production SPC system.
* This project does not use real semiconductor manufacturing data.
* This project does not reproduce any specific company’s internal dashboard.
* This project does not contain real tool IDs, recipes, control limits, or specification limits.
* This project does not determine root cause automatically.
* This project does not replace process, equipment, module, yield, or manufacturing engineering review.
* This project does not provide production disposition recommendations.

---

## Planned Methodology Extensions

Possible future phases may include:

### Phase 2 — Controlled methodology expansion

* Synthetic five-site thickness subgroup data
* Explicit X-bar/Sigma methodology demonstration
* Particle c-chart and u-chart analytical overlays

Cpk would remain an optional cross-functional view rather than a core module-level RTO monitor.

Particle count charts would remain analytical overlays and would not replace Phase 1 threshold alerts.

### Phase 3 — Engineering hardening

* Schema validation
* GitHub Actions CI
* Release validation
* Report export
* Dashboard polish
* Deployment documentation

### Phase 4 — Synthetic investigation layer

* Synthetic PM context
* Synthetic recipe-change context
* Synthetic metrology context
* Synthetic chamber context
* Deterministic event-to-context correlation
* Engineering investigation reports

This layer would identify candidate context only and would not determine root cause.

---

## Portfolio Skills Demonstrated

* Semiconductor process-monitoring methodology
* RTO tool-health analysis
* SPC limit calculation
* Particle-alert logic
* Across-fleet data visualization
* Event traceability
* Python package organization
* Synthetic data generation
* Streamlit application development
* Plotly visualization
* pytest validation
* Technical documentation
* Confidentiality-aware public portfolio design

---

## Interview Summary

A concise way to describe the project:

> I built a synthetic RTO SPC dashboard to demonstrate semiconductor tool-health monitoring workflows. The project uses RTR Mean, X-BAR, WIW Stdev, and SIGMA for thickness monitoring, along with Total, Cluster, and Large Particle Adder alerts. All limits are calculated from baseline data only for each tool, chamber, recipe, and metric stream. Across-fleet views support comparison without mixing SPC limits, and every warning or OOC signal is persisted into a traceable excursion-event table.

---

## License

Add the selected open-source license before public release.

Suggested options:

* MIT License
* Apache License 2.0

---

## Author

**Laurence Li**

Semiconductor Process Engineering | Manufacturing Analytics | SPC | Python
