# Schema Validation

`scripts/validate_data.py` validates the three Phase 1 CSV outputs:

- `monitor_measurements.csv`
- `spc_results.csv`
- `excursion_events.csv`

Validation checks required columns, allowed key values, non-empty files, and Phase 1 event method values.
